import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Understanding TEC — ZGIIS",
  description:
    "Plain-language guide to Total Electron Content — electrons, dual-frequency GPS, slant and vertical TEC, and Zimbabwe CORS maps.",
};

export default function UnderstandingTecLayout({ children }: { children: React.ReactNode }) {
  return children;
}
